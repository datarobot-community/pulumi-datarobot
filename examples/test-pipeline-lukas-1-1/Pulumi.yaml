name: docsassist_py
runtime:
  name: python
  options:
    toolchain: pip
    virtualenv: venv
description: A minimal Python Pulumi program
config:
  pulumi:tags:
    value:
      pulumi:template: python
  use_case:
    value:
      description: asdasd
      name: asd
plugins:
  providers:
    - name: datarobot
      path: ../../bin
