// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * Memory Space is a DataRobot concept that serves as a logical container for Chat Histories (Sessions) and persistent Memories. Managing a Memory Space requires either the `ENABLE_AGENTIC_MEMORY_API` or the `ENABLE_GENAI_EXPERIMENTATION` feature flag. Reading and writing persistent Memories in a Memory Space still requires `ENABLE_AGENTIC_MEMORY_API`.
 *
 * ## Example Usage
 *
 * <!--Start PulumiCodeChooser -->
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as datarobot from "@datarobot/pulumi-datarobot";
 *
 * const example = new datarobot.MemorySpace("example", {description: "My workspace memories"});
 * export const datarobotMemorySpaceId = example.id;
 * ```
 * <!--End PulumiCodeChooser -->
 */
export class MemorySpace extends pulumi.CustomResource {
    /**
     * Get an existing MemorySpace resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: MemorySpaceState, opts?: pulumi.CustomResourceOptions): MemorySpace {
        return new MemorySpace(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'datarobot:index/memorySpace:MemorySpace';

    /**
     * Returns true if the given object is an instance of MemorySpace.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is MemorySpace {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === MemorySpace.__pulumiType;
    }

    /**
     * Custom prompt instructions for fact extraction (maximum 10,000 characters). `None` means the default memory extraction prompt is used.
     */
    declare public readonly customInstructions: pulumi.Output<string | undefined>;
    /**
     * A human-readable description.
     */
    declare public readonly description: pulumi.Output<string | undefined>;
    /**
     * The chat API URL used for memory extraction. The memory service uses the DataRobot LLM gateway by default; set this only when the default does not work — for example, in air-gapped environments or when the required LLM model is not provided by the gateway and cannot be added.
     */
    declare public readonly llmBaseUrl: pulumi.Output<string | undefined>;
    /**
     * An LLM model name associated with the memory space (maximum 200 characters). Non-reasoning models are recommended. Reasoning-capable models are significantly slower for fact extraction without producing meaningfully better results.
     */
    declare public readonly llmModelName: pulumi.Output<string | undefined>;

    /**
     * Create a MemorySpace resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args?: MemorySpaceArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: MemorySpaceArgs | MemorySpaceState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as MemorySpaceState | undefined;
            resourceInputs["customInstructions"] = state?.customInstructions;
            resourceInputs["description"] = state?.description;
            resourceInputs["llmBaseUrl"] = state?.llmBaseUrl;
            resourceInputs["llmModelName"] = state?.llmModelName;
        } else {
            const args = argsOrState as MemorySpaceArgs | undefined;
            resourceInputs["customInstructions"] = args?.customInstructions;
            resourceInputs["description"] = args?.description;
            resourceInputs["llmBaseUrl"] = args?.llmBaseUrl;
            resourceInputs["llmModelName"] = args?.llmModelName;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(MemorySpace.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering MemorySpace resources.
 */
export interface MemorySpaceState {
    /**
     * Custom prompt instructions for fact extraction (maximum 10,000 characters). `None` means the default memory extraction prompt is used.
     */
    customInstructions?: pulumi.Input<string | undefined>;
    /**
     * A human-readable description.
     */
    description?: pulumi.Input<string | undefined>;
    /**
     * The chat API URL used for memory extraction. The memory service uses the DataRobot LLM gateway by default; set this only when the default does not work — for example, in air-gapped environments or when the required LLM model is not provided by the gateway and cannot be added.
     */
    llmBaseUrl?: pulumi.Input<string | undefined>;
    /**
     * An LLM model name associated with the memory space (maximum 200 characters). Non-reasoning models are recommended. Reasoning-capable models are significantly slower for fact extraction without producing meaningfully better results.
     */
    llmModelName?: pulumi.Input<string | undefined>;
}

/**
 * The set of arguments for constructing a MemorySpace resource.
 */
export interface MemorySpaceArgs {
    /**
     * Custom prompt instructions for fact extraction (maximum 10,000 characters). `None` means the default memory extraction prompt is used.
     */
    customInstructions?: pulumi.Input<string | undefined>;
    /**
     * A human-readable description.
     */
    description?: pulumi.Input<string | undefined>;
    /**
     * The chat API URL used for memory extraction. The memory service uses the DataRobot LLM gateway by default; set this only when the default does not work — for example, in air-gapped environments or when the required LLM model is not provided by the gateway and cannot be added.
     */
    llmBaseUrl?: pulumi.Input<string | undefined>;
    /**
     * An LLM model name associated with the memory space (maximum 200 characters). Non-reasoning models are recommended. Reasoning-capable models are significantly slower for fact extraction without producing meaningfully better results.
     */
    llmModelName?: pulumi.Input<string | undefined>;
}
