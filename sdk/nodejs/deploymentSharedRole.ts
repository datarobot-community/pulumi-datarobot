// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * Grants a directory group a role on a deployment, addressing the group by name rather than by ID.
 *
 * Pipelines know a group by the name it has in the identity provider, but the sharing API needs the internal group ID. This resource resolves the one to the other, so no group ID has to be hardcoded.
 *
 * Destroying the resource revokes the grant. Changing <span pulumi-lang-nodejs="`groupName`" pulumi-lang-dotnet="`GroupName`" pulumi-lang-go="`groupName`" pulumi-lang-python="`group_name`" pulumi-lang-yaml="`groupName`" pulumi-lang-java="`groupName`" pulumi-lang-hcl="`group_name`">`groupName`</span> revokes the previous group before granting the new one.
 *
 * ## Example Usage
 *
 * <!--Start PulumiCodeChooser -->
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as datarobot from "@datarobot/pulumi-datarobot";
 *
 * // Share a deployment with a group, addressing the group by the name it has in
 * // the identity provider rather than by its DataRobot ID. The name can come from
 * // a variable or the environment, so no ID is hardcoded in the stack.
 * const financeConsumer = new datarobot.DeploymentSharedRole("finance_consumer", {
 *     deploymentId: example.id,
 *     groupName: "FinanceTeam",
 *     role: "CONSUMER",
 * });
 * ```
 * <!--End PulumiCodeChooser -->
 */
export class DeploymentSharedRole extends pulumi.CustomResource {
    /**
     * Get an existing DeploymentSharedRole resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: DeploymentSharedRoleState, opts?: pulumi.CustomResourceOptions): DeploymentSharedRole {
        return new DeploymentSharedRole(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'datarobot:index/deploymentSharedRole:DeploymentSharedRole';

    /**
     * Returns true if the given object is an instance of DeploymentSharedRole.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is DeploymentSharedRole {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === DeploymentSharedRole.__pulumiType;
    }

    /**
     * The ID of the deployment to share.
     */
    declare public readonly deploymentId: pulumi.Output<string>;
    /**
     * The ID the group name resolved to.
     */
    declare public /*out*/ readonly groupId: pulumi.Output<string>;
    /**
     * The name of the group to grant the role to, matched in full and case sensitively. Changing this replaces the resource, so the previous group's grant is revoked before the new one is created.
     */
    declare public readonly groupName: pulumi.Output<string>;
    /**
     * The role to grant. One of `OWNER`, `USER` or `CONSUMER`. Defaults to `CONSUMER`, which allows predictions but does not make the deployment visible; use `USER` if the group needs to see it.
     */
    declare public readonly role: pulumi.Output<string>;

    /**
     * Create a DeploymentSharedRole resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: DeploymentSharedRoleArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: DeploymentSharedRoleArgs | DeploymentSharedRoleState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as DeploymentSharedRoleState | undefined;
            resourceInputs["deploymentId"] = state?.deploymentId;
            resourceInputs["groupId"] = state?.groupId;
            resourceInputs["groupName"] = state?.groupName;
            resourceInputs["role"] = state?.role;
        } else {
            const args = argsOrState as DeploymentSharedRoleArgs | undefined;
            if (args?.deploymentId === undefined && !opts.urn) {
                throw new Error("Missing required property 'deploymentId'");
            }
            if (args?.groupName === undefined && !opts.urn) {
                throw new Error("Missing required property 'groupName'");
            }
            resourceInputs["deploymentId"] = args?.deploymentId;
            resourceInputs["groupName"] = args?.groupName;
            resourceInputs["role"] = args?.role;
            resourceInputs["groupId"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(DeploymentSharedRole.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering DeploymentSharedRole resources.
 */
export interface DeploymentSharedRoleState {
    /**
     * The ID of the deployment to share.
     */
    deploymentId?: pulumi.Input<string | undefined>;
    /**
     * The ID the group name resolved to.
     */
    groupId?: pulumi.Input<string | undefined>;
    /**
     * The name of the group to grant the role to, matched in full and case sensitively. Changing this replaces the resource, so the previous group's grant is revoked before the new one is created.
     */
    groupName?: pulumi.Input<string | undefined>;
    /**
     * The role to grant. One of `OWNER`, `USER` or `CONSUMER`. Defaults to `CONSUMER`, which allows predictions but does not make the deployment visible; use `USER` if the group needs to see it.
     */
    role?: pulumi.Input<string | undefined>;
}

/**
 * The set of arguments for constructing a DeploymentSharedRole resource.
 */
export interface DeploymentSharedRoleArgs {
    /**
     * The ID of the deployment to share.
     */
    deploymentId: pulumi.Input<string>;
    /**
     * The name of the group to grant the role to, matched in full and case sensitively. Changing this replaces the resource, so the previous group's grant is revoked before the new one is created.
     */
    groupName: pulumi.Input<string>;
    /**
     * The role to grant. One of `OWNER`, `USER` or `CONSUMER`. Defaults to `CONSUMER`, which allows predictions but does not make the deployment visible; use `USER` if the group needs to see it.
     */
    role?: pulumi.Input<string | undefined>;
}
